//#Include <iostream>
//#Include <Vector>
//#include <algorithm>
//#Include <functional>
// int Main ()
// {{
// std :: Vector <int> Values ​​= {3,5,1,4,2};
// std :: SORT (VALUES.BEGIN (), VALUES.END (), STD :: GREATER <int> ());
// // For integer, the default promotion sequence is arranged, and the functional std :: Greater <NT> can be transformed into a descendant
// // You can also use the lambda function as follows as the lambda function will return True to indicate that the first parameter is in front of the array, and the backse indicates that the second parameter is in front of the array in front of the array
// std :: sort (values.begin (), valueS.end (), [] (int a, int b) {{
// if (a == 1)
// Return false;
// if (b == 1)
// Return true;
// Return a <b;
//});
// for (int Value: Values)
// std :: COUT << Value << STD :: Endl;
// std :: cin.get ();
// Return 0;
//}